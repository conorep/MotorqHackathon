import express from "express";

const router = express.Router();

/*router
    .post("/events", createEvent)
    .patch("/events", modifyEvent)
    .delete("/events/:eventID", deleteEvent)
    .post("/verify", eventVerification);

export default router;*/
