import express from "express";

const router = express.Router();

/*router
    /!*.get("/events", getAllEvents)*!/
    .get("/", getUserEvents)
    .post("/", registerUser);

export default router;*/
